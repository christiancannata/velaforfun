{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}


{% block title %}
    {% if title is defined %}
        {{ title }}
    {% else %}
        Crea annuncio scambio posto barca
    {% endif %}


{% endblock %}


{% block body %}


    <!-- contenuto -->
<div class="row clearfix">

     <div class="col-md-12 column">
        <div class="category">
            <a href="#" id="back-link" class="back-link"> <span class="glyphicon glyphicon-menu-left bordered" aria-hidden="true"></span>  </a>

            <h1>
                {% if title is defined %}
                    {{ title }}
                {% else %}
                    Nuovo annuncio scambio posto barca
                {% endif %}
                </h1>
        </div>
    </div>

    </div>

    <div class="row clearfix">

        <div class="col-md-8 column crud" >





    {{ form_start(form) }}
    {{ form_widget(form) }}
       <div>
        <input type="submit" class="btn btn-default login hvr-sweep-to-right"  value="Crea" />
    </div>
    <input type="hidden" class="callback" value="redirectScambioPosto">
    </form>

        </div>
        <div class="col-md-4 column">



            <div class="content">
                <div class="colonna-destra" style="margin-bottom: 20px">


                    <p>Compila i campi nel modo più preciso possibile, solo così potrai essere
                        contattato.<br>Se vorrai aggiungere delle informazioni al tuo annuncio o
                        rispondere a chi ti contatterà dovrai <a href="/register">iscriverti</a> gratuitamente alla
                        nostra community.</p>

                </div>


            </div>




        </div>
    </div>
    <!-- fine contenuto -->

</div>

{% endblock %}
